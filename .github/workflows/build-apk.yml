name: Build and Release APK
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.38.7'
        channel: 'stable'
    
    - name: Get dependencies
      run: flutter pub get
    
    - name: Build APK
      run: flutter build apk --release

    - name: Rename APK
      run: mv build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/ChanaFoodapp.apk

    
    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: ChanaFoodapp
        path: build/app/outputs/flutter-apk/ChanaFoodapp.apk
        retention-days: 30
    
    - name: Create Release
      if: github.event_name == 'push' && github.ref == 'refs/heads/master'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ github.run_number }}
        name: Release v${{ github.run_number }}
        body: |
          ## ChanapolAppFood
          
          **Developed by:** sutee-f, Chana321
          
          ### Download APK
          Download the APK file below to install on your Android device.
          
          **Build:** ${{ github.run_number }}
          **Date:** ${{ github.event.head_commit.timestamp }}
        files: build/app/outputs/flutter-apk/ChanaFoodapp.apk
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}